import Option from "./Option";

const Category = () => {
    return (
        <section class="menu">
            <h1 class="menu__title">Primeiro, seu prato</h1>
            <ul class="menu__options">
                <Option />
            </ul>
        </section>
    );
};

export default Category;
